<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
     
<mapper namespace="com.example.mountain.repository.UserMapper">

	<insert id="insertUser">
		INSERT INTO mt_users(
			username,
			password)
		VALUES(
			#{username},
			#{password})
	</insert>
	
	<select id="findByUsername" resultType="com.example.mountain.entity.MtUser">
		SELECT * FROM mt_users 
		WHERE 
			username = #{username}
	</select>
	
	<update id="deleteUser">
		UPDATE mt_users 
		SET
			deleted_at = now()
		WHERE 
			username = #{username}
	</update>	
	
	<update id="updateUser">
		UPDATE mt_users 
		SET
			username = #{username},
			password = #{password}
		WHERE 
			id = #{id}
	</update>
	
	
	
</mapper>